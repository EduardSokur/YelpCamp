<%- include("../partials/header") %>

<div class="container">

        <h1 id="newCampH1">Edit comment</h1>
        
        <form action="/campgrounds/<%= campground_id %>/comments/<%= comment._id %>?_method=PUT" method="POST">
                <div class="form-group row">
                    <label for="newCampgroundNameInput">Text</label>
                    <input class="form-control" type="text" value="<%= comment.text %>" name="comment[text]" id="newCampgroundNameInput">
                </div>
                    <div class="form-group row">
                    <button class="btn btn-primary btn-block">Submit</button>
                </div>
                </div>
        </form>
        
        </div>

<%- include("../partials/footer") %>